@using Stargazer.Models
@model List<Picture>

@if (Model.Count() <= 0)
{
    <p>There are no pictures for this body</p>
}
else
{
    @foreach(Picture picture in Model)
    {
        <img src="@picture.Link"/>
    }
}
